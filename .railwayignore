# Railway only serves the static frontend via server.js.
# Keep the backend/agent code out of the build context so Nixpacks
# doesn't get confused by Python or Dockerfile signals.
bridge/
deploy/
cli/
bin/
Dockerfile
.dockerignore
*.md
.git/

# Design reference comps — large SVG mockups, not part of the served site.
Frame 1.svg
Mobile - Home.svg

